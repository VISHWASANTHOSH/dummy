name: Merge Feature Branch

on:
  push:
    branches:
      - ahamed

jobs:
  merge-feature:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Merge Feature into Ahamed
      run: |
        git config user.name "Santhosh-Nagrajan"
        git config user.email "nsmgbros001@github.com"

        git checkout ahamed
        git merge origin/feature

        git push origin ahamed

